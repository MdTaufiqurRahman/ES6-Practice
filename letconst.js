const hubby = "Elias Kanchon";
hubby = 'anik'; // const value can not be change
console.log(hubby);

const numbers = [12, 15];
numbers [1] = 88;
numbers.push(13);
numbers = ['sunny', 'omar', 'amir' ]; //Not change the full array.only change element and push pop.
console.log(numbers);

let patientName = 'Raahim Chacha';
patientName = "Fatema Khala"  // if use let then we can change the value.
console.log(patientName);

//let called scope variable.